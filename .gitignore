# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
authkey*
*-authkey*

# Per-box context is private — keep real servers out of a public repo.
# servers/ ships empty; every box folder you add stays untracked.
servers/*/

# Generated / local
*.rendered.sh
scratch/
.DS_Store
TAILSCALE-SSH-MIGRATION.md
HQ-FINDINGS-*.md

# Copy to .env and fill in. .env is gitignored — never commit real values.

# --- Hetzner ---
# Create at: Hetzner Cloud Console > Security > API Tokens (Read & Write)
HETZNER_API_TOKEN=

# --- Tailscale ---
# Create at: Tailscale admin > Settings > Keys > Generate API key
TAILSCALE_API_KEY=
